# SQL Server connection string.  Using Windows Security to access the DB.  
#  Whatever account running the app must have access to the DB too
##spring.datasource.url=jdbc:sqlserver://localhost;databaseName=springbootdb;integratedSecurity=true;
##spring.datasource.driverClassName=com.microsoft.sqlserver.jdbc.SQLServerDriver
##spring.jpa.show-sql=true
##spring.jpa.hibernate.ddl-auto = update
# postgreSQL database
spring.datasource.url= jdbc:postgresql://localhost:5432/postgres
spring.datasource.username=postgres
spring.datasource.password=SuperUser@1199   
spring.jpa.hibernate.ddl-auto=update
# Disable feature detection by this undocumented parameter. Check the org.hibernate.engine.jdbc.internal.JdbcServiceImpl.configure method for more details.
spring.jpa.properties.hibernate.temp.use_jdbc_metadata_defaults = false
# running on 8090
# uncomment to run on 8090
server.port = 8090
## MULTIPART (MultipartProperties)
# Enable multipart uploads
spring.servlet.multipart.enabled=true
# Threshold after which files are written to disk.
spring.servlet.multipart.file-size-threshold=2KB
# Max file size.
spring.servlet.multipart.max-file-size=200MB
# Max Request Size
spring.servlet.multipart.max-request-size=215MB

## File Storage Properties
# All files uploaded through the REST API will be stored in this directory
file.upload-dir=./uploads

## Mail server information
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=cccinnerhealing@gmail.com
spring.mail.password=Bunker99
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true

# security logging
logging.level.org.springframework.security=DEBUG

# App Properties
grokonez.app.jwtSecret=jwtGrokonezSecretKey
grokonez.app.jwtExpiration=86400


